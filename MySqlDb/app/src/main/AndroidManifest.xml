<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mysqldb">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GoodsSearchActivity" />
        <activity android:name=".GoodsAddActivity" />
        <activity android:name=".GoodsEditActivity" />
        <activity android:name=".GoodsManagerActivity" />
        <activity android:name=".GoodsManageActivity" />
        <activity android:name=".UserEditActivity" />
        <activity android:name=".UserAddActivity" />
        <activity android:name=".UserManagerActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>